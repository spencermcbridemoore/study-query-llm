#!/bin/sh
# Living-docs-only governance pre-commit hook (blocking; fast static/staged checks).
# The full test suite runs in CI, not here.
# Install: git config core.hooksPath scripts/git-hooks
set -e
python scripts/check_staged_restricted_paths.py
python scripts/verify_script_path_references.py
python scripts/check_persistence_contract.py
python scripts/check_new_run_scripts_have_tests.py
