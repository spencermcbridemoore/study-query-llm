# Documentation Parity Ledger

Status: living  
Owner: documentation-maintainers  
Last reviewed: 2026-04-26

## Purpose

This ledger tracks concrete documentation claims against repository evidence.
It is the evidence base for documentation updates and taxonomy decisions.

Status values:

- `verified`: matches current code/tests behavior.
- `partial`: mostly true but incomplete/outdated scope.
- `stale`: formerly true but no longer current.
- `incorrect`: factually wrong for current code.
- `superseded`: replaced by a newer canonical path.
- `ambiguous`: cannot be statically verified from repository content alone.

## Claim Ledger

| claim_id | source_doc | claim_text | evidence_paths | status | impact | action |
|---|---|---|---|---|---|---|
| C001 | `README.md` | "Production Ready - All core features implemented and tested." | `README.md`, `docs/IMPLEMENTATION_PLAN.md` | superseded | High: previously overstated readiness. | Completed: replaced with current-state references and historical roadmap links. |
| C002 | `README.md` | Completed phases include OpenAI + Hyperbolic provider implementation and full provider factory support. | `README.md`, `src/study_query_llm/providers/factory.py` | superseded | High: previously implied outdated provider model. | Completed: phased completion claims removed from README. |
| C003 | `README.md` | "Full test coverage (112 tests)." | `README.md`, `tests/**/*.py` | superseded | Medium: previously stale quality signal. | Completed: hardcoded test-count claims removed. |
| C004 | `docs/ARCHITECTURE.md` | Data access layer centers `InferenceRepository` for current analytics flows. | `docs/ARCHITECTURE.md`, `panel_app/views/analytics.py`, `src/study_query_llm/services/study_service.py` | superseded | High: legacy architecture narrative can mislead if treated as current. | Mitigated by routing: current architecture moved to `docs/living/ARCHITECTURE_CURRENT.md`; source doc marked historical. |
| C005 | `docs/ARCHITECTURE.md` | v2 database section lists `InferenceRun` fields under `models_v2.py`. | `docs/ARCHITECTURE.md`, `src/study_query_llm/db/models_v2.py` | superseded | High: schema confusion risk in legacy narrative. | Mitigated by routing and historical labeling; use living architecture doc for implementation decisions. |
| C006 | `docs/ARCHITECTURE.md` | Langfuse is integrated as observability layer in current app architecture. | `docs/ARCHITECTURE.md`, search `langfuse` (no source matches under `src/`) | superseded | Medium: outdated expectation in historical narrative. | Mitigated by living-doc routing; retain as historical context only. |
| C007 | `docs/API.md` | `ProviderFactory.get_available_providers()` returns `['azure','openai','hyperbolic']`. | `docs/API.md`, `src/study_query_llm/providers/factory.py` | superseded | High: stale API in deprecated doc. | Completed: `docs/API.md` marked deprecated; canonical API moved to `docs/living/API_CURRENT.md`. |
| C008 | `docs/API.md` | `StudyService` uses `InferenceRepository` in normal usage examples. | `docs/API.md`, `src/study_query_llm/services/study_service.py`, `panel_app/views/analytics.py` | superseded | High: stale API usage in deprecated doc. | Completed: canonical v2 usage documented in `docs/living/API_CURRENT.md`. |
| C009 | `docs/API.md` | `ProviderResponse.metadata` defaults to `None`. | `docs/API.md`, `src/study_query_llm/providers/base.py` | superseded | Low: minor stale API detail in deprecated doc. | Accepted as deprecated-doc drift; canonical reference is `docs/living/API_CURRENT.md`. |
| C010 | `docs/IMPLEMENTATION_PLAN.md` | Phase 1.3/1.4 openai/hyperbolic provider implementation is not implemented. | `docs/IMPLEMENTATION_PLAN.md`, `src/study_query_llm/providers/openai_compatible_chat_provider.py`, `src/study_query_llm/providers/factory.py`, `src/study_query_llm/config.py` | superseded | Medium: historical phase chronology diverges from current implementation. | Completed: plan explicitly labeled historical-roadmap; current status moved to `docs/living/CURRENT_STATE.md`. |
| C011 | `docs/IMPLEMENTATION_PLAN.md` | Phase 4.1 says StudyService wraps `InferenceRepository`. | `docs/IMPLEMENTATION_PLAN.md`, `src/study_query_llm/services/study_service.py` | superseded | Medium: legacy phase narrative not aligned with v2 defaults. | Mitigated by historical labeling + living current-state docs. |
| C012 | `docs/DEPLOYMENT.md` + `Dockerfile` | Build-time tests run `pytest tests/test_e2e_verification.py`. | `docs/DEPLOYMENT.md`, `Dockerfile`, `test_e2e_verification.py` | superseded | Medium: previously mismatched command path. | Completed: build-time smoke gate now uses `pytest tests/test_scripts/test_report_layer0_dataset_stats.py` in both docs and Dockerfile. |
| C013 | `docs/TESTING_CHECKLIST.md` | E2E verification script reflects current database model path. | `docs/TESTING_CHECKLIST.md`, `test_e2e_verification.py`, `src/study_query_llm/db/raw_call_repository.py` | superseded | Medium: previously implied v1 script as current. | Completed: checklist now explicitly flags `test_e2e_verification.py` as legacy-v1 validation. |
| C014 | `docs/STANDING_ORDERS.md` | `IMPLEMENTATION_PLAN.md` and `ARCHITECTURE.md` are sole authoritative truth for status/design. | `docs/STANDING_ORDERS.md` | superseded | High: previously pointed process at drifting docs. | Completed: standing orders now point to living docs + parity ledger. |
| C015 | `docs/USER_GUIDE.md` | Contains both v1 and v2 setup in one path, presented as equivalent current flow. | `docs/USER_GUIDE.md`, `docs/history/USER_GUIDE_V1_LEGACY.md` | superseded | Medium: previously mixed onboarding path. | Completed: v2-first guide retained; legacy v1 content split to historical doc and v2 analytics examples updated. |
| C016 | `docs/ARCHITECTURE.md` | Standalone sweep worker + package CLI routes are canonical operational path. | `docs/ARCHITECTURE.md`, `src/study_query_llm/cli/__main__.py`, `src/study_query_llm/services/jobs/runtime_workers.py`, `src/study_query_llm/experiments/runtime_sweeps.py` | verified | Medium positive: valid anchor for operations docs. | Keep as canonical in living architecture summary. |
| C017 | `docs/SWEEP_MIGRATION_RUNBOOK.md` | CLI-first recommendation with script wrappers as compatibility layer. | `docs/SWEEP_MIGRATION_RUNBOOK.md`, `scripts/run_300_bigrun_sweep.py`, `scripts/run_langgraph_job_worker.py` | verified | Medium positive: correct operator guidance. | Keep as runbook; tighten cross-links from docs index. |
| C018 | `docs/LOCAL_DB_CLONE_FROM_JETSTREAM.md` | Jetstream clone runbook uses explicit backup + restore + verify phases. | `docs/LOCAL_DB_CLONE_FROM_JETSTREAM.md`, referenced scripts under `scripts/` | verified | Low positive: clear and operationally useful runbook. | Keep as runbook lane with minimal edits. |
| C019 | `docs/BLOB_OPS_HARDENING_POLICY.md` | Blob policy defines lane/auth/strict-mode governance contract. | `docs/BLOB_OPS_HARDENING_POLICY.md`, `src/study_query_llm/storage/azure_blob.py`, `src/study_query_llm/services/artifact_service.py` | verified | Medium positive: hard quota and backend governance now enforced in write path. | Keep as living policy; monitor operational exceptions. |
| C020 | `docs/MIGRATION_GUIDE.md` | Migration from ad-hoc scripts to pytest is current primary guidance. | `docs/MIGRATION_GUIDE.md`, `CONTRIBUTING.md` | superseded | Low: duplicate guidance and old migration framing. | Completed: marked deprecated and routed to current testing/contributing guidance. |
| C021 | `docs/living/CURRENT_STATE.md` + `docs/living/ARCHITECTURE_CURRENT.md` | OrchestrationJob-first clustering path and unified `provenanced_runs` execution model are current behavior. | `src/study_query_llm/services/sweep_request_service.py`, `src/study_query_llm/services/provenanced_run_service.py`, `src/study_query_llm/db/models_v2.py`, `src/study_query_llm/experiments/sweep_worker_main.py` | verified | High positive: aligns docs with control-plane and provenance contracts used by runtime code. | Keep as canonical architecture contract for ongoing migration/cutover work. |
| C022 | `docs/living/CURRENT_STATE.md` + `docs/living/ARCHITECTURE_CURRENT.md` | MCQ runs are OrchestrationJob-first (single `mcq_run` job per run_key), and new MCQ method executions persist explicit `provenanced_runs` rows while legacy MCQ rows remain queryable via compatibility mapping. | `src/study_query_llm/services/sweep_request_service.py`, `src/study_query_llm/services/jobs/job_payload_models.py`, `src/study_query_llm/services/jobs/job_runner_factory.py`, `src/study_query_llm/experiments/sweep_worker_main.py`, `src/study_query_llm/experiments/mcq_run_persistence.py`, `src/study_query_llm/services/provenanced_run_service.py` | verified | High positive: aligns MCQ execution/provenance contracts with the same control-plane/query model used by clustering. | Keep as canonical until dedicated MCQ backfill policy changes. |
| C023 | `docs/DATA_PIPELINE.md` + `docs/living/CURRENT_STATE.md` | Snapshot provenance uses canonical `depends_on` lineage (`analysis_run -> embedding_batch -> dataset_snapshot -> dataset`) with stage-level idempotent reuse. | `src/study_query_llm/pipeline/snapshot.py`, `src/study_query_llm/pipeline/embed.py`, `src/study_query_llm/pipeline/analyze.py`, `src/study_query_llm/pipeline/runner.py`, `scripts/run_bank77_pipeline.py` | superseded | Medium: lineage model changed when parse became canonical stage and embed moved to dataframe dependency. | Completed: superseded by five-stage lineage claims C047/C048. |
| C024 | `docs/DATA_PIPELINE.md` + `docs/runbooks/README.md` + `docs/living/CURRENT_STATE.md` | BANK77 operator flow runs through `scripts/run_bank77_pipeline.py` and validates full-data stage outputs plus second-run idempotent reuse. | `scripts/run_bank77_pipeline.py`, `tests/pipeline/test_bank77_pipeline_e2e.py`, `src/study_query_llm/datasets/source_specs/banking77.py`, `src/study_query_llm/pipeline/*` | superseded | Medium: claim text references older four-stage surface. | Completed: replaced by five-stage BANK77 contract claim C048. |
| C025 | `docs/living/CURRENT_STATE.md` + `docs/living/API_CURRENT.md` | OpenRouter is first-class in embedding provider surfaces, deployment discovery supports OpenRouter runtime catalogs, ModelRegistry persists deployment metadata, and embedding token validation can consume discovered context-length limits with fallback behavior. | `src/study_query_llm/providers/factory.py`, `src/study_query_llm/providers/base.py`, `src/study_query_llm/services/model_registry.py`, `src/study_query_llm/services/embeddings/service.py`, `tests/test_providers/test_factory.py`, `tests/test_services/test_model_registry.py`, `tests/test_services/test_embedding_service.py` | verified | High positive: closes chat-only/discovery gaps for OpenRouter embeddings and makes runtime characterization available through canonical surfaces. | Keep as canonical behavior; extend metadata/probing only if stronger provider guarantees are required. |
| C026 | `docs/living/CURRENT_STATE.md` + `docs/living/ARCHITECTURE_CURRENT.md` + `docs/living/API_CURRENT.md` | Run/analysis reunification is now execution-first: canonical `provenanced_runs.run_kind=execution`, MCQ `analysis_run` jobs share orchestration control plane, and request analysis state reads derive from execution/job evidence with parity diagnostics vs legacy metadata. | `src/study_query_llm/services/provenanced_run_service.py`, `src/study_query_llm/db/raw_call_repository.py`, `src/study_query_llm/db/models_v2.py`, `src/study_query_llm/services/sweep_request_service.py`, `src/study_query_llm/experiments/sweep_worker_main.py`, `src/study_query_llm/analysis/mcq_analyze_request.py`, `src/study_query_llm/services/jobs/job_payload_models.py` | verified | High positive: aligns living docs with the unified execution substrate and removes split control/read semantics from canonical behavior. | Keep as canonical contract; remove legacy mirrors after migration window + backfill validation. |
| C027 | `docs/living/CURRENT_STATE.md` | Panel includes a Storage / DB stats tab: Postgres catalog sizes and largest relations when `DATABASE_URL` is PostgreSQL; v2 counts including `CallArtifact`; artifact env summary (no secrets); optional capped Azure blob prefix listing; redacted `LOCAL_DATABASE_URL` and clone runbook pointers. | `panel_app/app.py`, `panel_app/views/storage_stats.py`, `src/study_query_llm/storage/azure_blob.py` | verified | Low positive: operator visibility without expanding write paths. | Keep tab behavior aligned with artifact env contract in `artifact_service.py` / blob policy docs. |
| C028 | `docs/DATASET_ACQUISITION_LAYER0.md` + `docs/living/CURRENT_STATE.md` | Layer-0 + `record_dataset_download.py` are the active acquisition contract. | `docs/DATASET_ACQUISITION_LAYER0.md`, `scripts/record_dataset_download.py` | superseded | Medium: this path was retired during four-stage pipeline cutover. | Completed: old doc/script deleted; acquisition now documented in `docs/DATA_PIPELINE.md` and executed through pipeline stages. |
| C029 | `docs/DATASET_ACQUISITION_LAYER0.md` + `docs/living/CURRENT_STATE.md` | Layer-0 registry details are part of the canonical operator narrative. | `docs/DATASET_ACQUISITION_LAYER0.md` | superseded | Low: Layer-0 naming retired from living operator guidance. | Completed: folded into `docs/DATA_PIPELINE.md`; historical details remain in code/tests under `datasets/source_specs/`. |
| C030 | `deploy/jetstream/RUNBOOK.md` | Jetstream HTTPS failures: replace literal `YOUR_DOMAIN` with forward-resolvable DNS; for IP-only access document `tls internal` plus global `default_sni` for clients that omit SNI on IPv4; `PANEL_ALLOW_WS_ORIGINS` must use host/port only (no URL scheme) for Bokeh. | `deploy/jetstream/RUNBOOK.md`, `panel_app/app.py` | verified | Medium positive: closes common Jetstream2 TLS + Panel WS misconfiguration loop. | Keep aligned with on-VM `/etc/caddy/Caddyfile` practices. |
| C031 | `docs/living/CURRENT_STATE.md` | Panel sidebar Database health lists v2 counts for groups, raw_calls, provenanced_runs, mcq_run groups, and call_artifacts; clarifies MCQ vs raw_calls semantics and points operators to Storage / DB stats for catalog/Azure detail. | `panel_app/helpers.py`, `docs/living/CURRENT_STATE.md` | verified | Low positive: reduces false empty-DB reads when MCQ/provenance/artifacts exist without chat-style raw_calls. | Keep counts cheap (session COUNT); revisit if very large DBs need approximate metrics. |
| C032 | `docs/living/CURRENT_STATE.md` + `docs/living/API_CURRENT.md` | Panel Analytics tab surfaces v2 counts (including `group_members`), raw_calls modality/status histograms, groups by group_type, recent provenanced_runs/groups, and recent raw_calls with optional StudyService filters. | `panel_app/views/analytics.py`, `src/study_query_llm/services/study_service.py`, `tests/test_services/test_study_service.py` | verified | Low positive: docs now match post-`embedding_vectors` schema removal. | Keep queries bounded (limits); revisit if large DBs need pagination. |
| C033 | `deploy/jetstream/RUNBOOK.md` + `deploy/jetstream/README.md` | Jetstream redeploy script `redeploy_panel_from_origin.sh` runs git pull on the VM repo, optionally sets `IMAGE_REF` from `NEW_IMAGE_REF` (Python reads `.env.jetstream` bytes as UTF-8 with `errors='replace'`), `docker compose pull app` + `up -d` with `--env-file .env.jetstream`, then curls Panel `/health`. | `deploy/jetstream/redeploy_panel_from_origin.sh` | verified | Low positive: single operator path for compose-on-disk + container image refresh. | Keep aligned with compose project name and env-file flags in `docker-compose.jetstream.yml`. |
| C034 | `scratch/local/README.md` + `.gitignore` | `scratch/local/*` is gitignored except `scratch/local/README.md` and `scratch/local/jetstream-remote-build-and-restart.ps1`; README documents the Windows driver (SSH: stop app, git pull, VM `docker build`, Python-only `IMAGE_REF` rewrite with `read_bytes`/`decode(..., errors='replace')` for non-UTF-8 `.env.jetstream`, `up --pull never --force-recreate app`, health curl). | `scratch/local/README.md`, `scratch/local/jetstream-remote-build-and-restart.ps1`, `.gitignore` | verified | Low positive: Path-A VM build workflow from a Windows workstation without putting secrets in the script. | Keep character-set validation in the ps1 in sync with README examples. |
| C035 | `docs/runbooks/README.md` + `scripts/README.md` + DB ops scripts | Canonical DB ops flow through one runbook index with explicit `DATABASE_URL`/`JETSTREAM_DATABASE_URL`/`LOCAL_DATABASE_URL` contract, and high-risk scripts enforce target guardrails (`restore_pg_dump_to_local_docker.py`, `sync_from_online.py`, `purge_dataset_acquisition.py`) including remote-target overrides and destructive confirmations. | `docs/runbooks/README.md`, `scripts/README.md`, `scripts/db_target_guardrails.py`, `scripts/restore_pg_dump_to_local_docker.py`, `scripts/sync_from_online.py`, `scripts/purge_dataset_acquisition.py`, `tests/test_scripts/test_*guardrails.py` | verified | High positive: materially reduces wrong-target DB write/delete risk and removes conflicting source-of-truth narratives. | Keep guardrail flags in runbook examples whenever destructive/scripted writes are documented. |
| C036 | `scripts/README.md` + `docs/living/CURRENT_STATE.md` | Script governance now uses lane taxonomy (`scripts/living`, `scripts/history`, `scripts/deprecated`, `scripts/internal`), with a first pass moving no-PCA/experimental scripts into `scripts/history` while keeping root compatibility wrappers, plus a recurring active-surface reference check via `scripts/verify_script_path_references.py`. | `scripts/README.md`, `scripts/history/README.md`, `scripts/history/analysis/*.py`, `scripts/history/experiments/*.py`, `scripts/history/one_offs/*.py`, root wrapper scripts for moved entrypoints, `scripts/verify_script_path_references.py`, `tests/test_scripts/test_verify_script_path_references.py`, `docs/living/CURRENT_STATE.md` | verified | Medium positive: reduces top-level operational ambiguity while preserving command compatibility during migration. | Move set v1.1: thin root wrappers forward to `scripts/deprecated/*.py` (then history or `run_pca_kllmeans_sweep`); sweep recovery scripts under `scripts/history/sweep_recovery/`. Move set v1.2: one-off probes/utilities moved to `scripts/history/one_offs/` with wrappers only when active references require path stability. |
| C037 | `scripts/README.md` + `docs/runbooks/README.md` + `docs/living/CURRENT_STATE.md` | Jetstream-sourced `pg_dump -Fc` files can be uploaded to Azure blob container `db-backups` via `scripts/upload_jetstream_pg_dump_to_blob.py`, with companion manifests under `backup_pg_dumps/` for `scripts/verify_db_backup_inventory.py`. | `scripts/upload_jetstream_pg_dump_to_blob.py`, `scripts/verify_db_backup_inventory.py`, `tests/test_scripts/test_upload_jetstream_pg_dump_to_blob.py` | verified | Medium positive: off-VM backup alongside existing five-container layout without new storage accounts. | Keep flat blob names (dump basename) so inventory matching stays stable. |
| C038 | `scripts/README.md` + `docs/runbooks/README.md` + `docs/living/CURRENT_STATE.md` | Operators can verify Azure-backed `call_artifacts.uri` rows target an expected blob container (default `artifacts-dev`) and optional key prefix via `scripts/verify_call_artifact_blob_lanes.py` (read-only SELECT). | `scripts/verify_call_artifact_blob_lanes.py`, `tests/test_scripts/test_verify_call_artifact_blob_lanes.py` | verified | Low positive: cheap drift detection across DB clones without listing Azure manually. | Keep URL parsing aligned with `AzureBlobStorageBackend` blob URL shape. |
| C039 | `docs/DATASET_ACQUISITION_LAYER0.md` + `docs/runbooks/record_dataset_download.md` | Layer-0 measured-stats runbook is part of the active docs surface. | `docs/DATASET_ACQUISITION_LAYER0.md`, `docs/runbooks/record_dataset_download.md` | superseded | Low: entire Layer-0 doc/runbook surface was retired in PR-7 docs reorg. | Completed: removed obsolete docs and switched runbook pointers to `docs/DATA_PIPELINE.md` + `scripts/run_bank77_pipeline.py`. |

| C040 | `docs/living/CURRENT_STATE.md` + `docs/living/ARCHITECTURE_CURRENT.md` + `docs/living/SCHEDULING_PROVENANCE_BOUNDARY.md` + `docs/STANDING_ORDERS.md` | `ProvenancedRun` carries `fingerprint_json`/`fingerprint_hash` for scheduling-independent algorithmic identity; scheduling vs provenance boundary rule is documented; orchestration claim/complete paths have perf_counter timing. | `src/study_query_llm/db/models_v2.py`, `src/study_query_llm/services/provenanced_run_service.py`, `src/study_query_llm/db/raw_call_repository.py`, `src/study_query_llm/experiments/sweep_worker_main.py`, `src/study_query_llm/db/migrations/add_fingerprint_columns.py`, `scripts/backfill_run_fingerprints.py`, `tests/test_services/test_run_fingerprint.py` | verified | High positive: enables semantic comparison of runs independent of orchestration shape, formalizes granularity boundary, and instruments overhead. | Keep fingerprint field list aligned with method taxonomy expansion. |
| C041 | `docs/living/METHOD_RECIPES.md` + `docs/living/CURRENT_STATE.md` + `docs/living/ARCHITECTURE_CURRENT.md` + `docs/STANDING_ORDERS.md` | `MethodDefinition` carries an optional `recipe_json` column; `algorithms/recipes.py` registers five clustering component methods (`mean_pool_tokens`, `pca_svd_project`, `kmeanspp_init`, `k_llmmeans`, `umap_project`) while the canonical `cosine_kllmeans_no_pca` recipe is a three-stage composite (`mean_pool_tokens` -> `kmeanspp_init` -> `k_llmmeans`); clustering ingestion injects `canonical_recipe_hash(recipe)` into run `config_json["recipe_hash"]` so the canonical run fingerprint absorbs recipe identity without a fingerprint tuple shape change. | `src/study_query_llm/db/models_v2.py`, `src/study_query_llm/db/migrations/add_recipe_json_column.py`, `src/study_query_llm/algorithms/recipes.py`, `src/study_query_llm/algorithms/__init__.py`, `src/study_query_llm/services/method_service.py`, `src/study_query_llm/experiments/ingestion.py`, `scripts/register_clustering_methods.py`, `tests/test_services/test_recipe.py` | verified | High positive: makes composite/pipeline identity explicit at the provenance layer and lets fingerprints distinguish structurally different pipelines without breaking existing fingerprints. | Extend `COMPOSITE_RECIPES` as new composites are introduced; bump composite `version` when the recipe shape or component identities change. |
| C042 | `docs/DATA_PIPELINE.md` + `docs/README.md` + `AGENTS.md` | `docs/DATA_PIPELINE.md` is now the canonical pipeline reference, and retired Layer-0/Layer-1 docs are removed from active navigation. | `docs/DATA_PIPELINE.md`, `docs/README.md`, `AGENTS.md`, `docs/runbooks/README.md` | verified | Medium positive: removes split documentation lanes and centralizes operator/developer guidance on one pipeline contract. | Keep downstream links pointed at `docs/DATA_PIPELINE.md` when adding pipeline features or runbook examples. |
| C043 | `docs/living/CURRENT_STATE.md` + `docs/DATA_PIPELINE.md` | `sources_uncertainty_qc` is parser-backed in stage-2 `parse` (`parse_sources_uncertainty_snapshot`) and supports a PM-only parser variant (`parse_sources_uncertainty_pm_snapshot`), enabling stage-3 snapshot variants with distinct parser identity/representation provenance metadata. | `src/study_query_llm/datasets/source_specs/sources_uncertainty_zenodo.py`, `src/study_query_llm/datasets/source_specs/registry.py`, `tests/test_datasets/test_sources_uncertainty_parser.py`, `tests/pipeline/test_sources_uncertainty_snapshot.py`, `src/study_query_llm/pipeline/snapshot.py` | verified | Medium positive: unlocks immediate PM-focused analysis without blocking BM/SG/SS expansion and preserves normalization provenance for comparison. | Keep label-code mapping and subset-profile metadata stable; version parser behavior explicitly if taxonomy/filter policy changes. |
| C044 | `docs/living/CURRENT_STATE.md` | Phase-1 unknown-`k` analysis is available through stage-5 `analyze` using `hdbscan_runner` (including BANK77 script surface `--analysis-strategy hdbscan`), and analyze finalization enriches the same execution row with canonical config/fingerprint fields via `record_analysis_execution(..., analysis_run_key=run_key)`; full unknown-`k` sweep-worker/ingestion integration is explicitly deferred to phase-2. | `src/study_query_llm/pipeline/hdbscan_runner.py`, `src/study_query_llm/pipeline/analyze.py`, `scripts/run_bank77_pipeline.py`, `tests/pipeline/test_analyze.py`, `tests/pipeline/test_bank77_pipeline_e2e.py`, `docs/living/CURRENT_STATE.md` | superseded | Medium: claim is now subsumed by clustering provenance v1 multi-method rollout. | Replaced by C056 (v1 clustering provenance + canonical runners). |
| C045 | `docs/living/CURRENT_STATE.md` | `ausem` is parser-backed in stage-2 `parse` with a default all-problems parser (`parse_ausem_snapshot`) and four per-problem profiles (`parse_ausem_problem1_snapshot` ... `parse_ausem_problem4_snapshot`), enabling stage-3 combined/per-problem snapshots with distinct parser identity and representation provenance metadata. | `src/study_query_llm/datasets/source_specs/ausem.py`, `src/study_query_llm/datasets/source_specs/registry.py`, `tests/test_datasets/test_ausem_parser.py`, `tests/pipeline/test_ausem_snapshot.py`, `src/study_query_llm/pipeline/snapshot.py` | verified | Medium positive: enables direct educational-scope slicing without re-acquisition while preserving parser-level provenance for comparisons. | Keep required-column and correctness-label normalization stable; version parser profiles explicitly if AuSeM schema/label policy changes. |
| C046 | `docs/living/CURRENT_STATE.md` + `docs/DATA_PIPELINE.md` | Stage-3 label-level pooling now uses canonical representation name `label_centroid` (legacy alias `intent_mean` remains accepted), and stage-4 HDBSCAN analysis enforces `full` embeddings to avoid clustering on label-centroid aggregates. | `src/study_query_llm/pipeline/embed.py`, `src/study_query_llm/pipeline/hdbscan_runner.py`, `scripts/run_bank77_pipeline.py`, `tests/pipeline/test_embed.py`, `tests/pipeline/test_analyze.py`, `docs/living/CURRENT_STATE.md` | superseded | Medium: non-full pooling moved out of embed into analyze-time derivation. | Completed: replaced by five-stage representation split claim C047. |
| C047 | `docs/living/CURRENT_STATE.md` + `docs/DATA_PIPELINE.md` | Five-stage pipeline is canonical (`acquire -> parse -> snapshot -> embed -> analyze`); parse writes `dataset_dataframe`, snapshot writes deterministic `dataset_subquery_spec`, embed is dataframe-scoped full-only (`dataframe:<id>:full`), and analyze is dual-input (`snapshot_group_id`, `embedding_batch_group_id`) with representation-aware fingerprinting. | `src/study_query_llm/pipeline/parse.py`, `src/study_query_llm/pipeline/snapshot.py`, `src/study_query_llm/pipeline/embed.py`, `src/study_query_llm/pipeline/analyze.py`, `src/study_query_llm/pipeline/types.py`, `scripts/run_bank77_pipeline.py`, `tests/pipeline/test_parse.py`, `tests/pipeline/test_snapshot.py`, `tests/pipeline/test_embed.py`, `tests/pipeline/test_analyze.py` | verified | High positive: aligns canonical docs with the implemented five-stage contracts and determinism/idempotency boundaries. | Keep this as the canonical pipeline contract; update only when stage contracts or group/artifact identities change. |
| C048 | `docs/DATA_PIPELINE.md` + `docs/living/CURRENT_STATE.md` + `docs/review/DOC_PARITY_LEDGER.md` | BANK77 reference flow is five-stage and validated through pipeline tests plus full-suite regression (`pytest`), with idempotent reuse asserted across parse/snapshot/embed/analyze contracts. | `scripts/run_bank77_pipeline.py`, `tests/pipeline/test_bank77_pipeline_e2e.py`, `tests/pipeline/test_acquire_snapshot_chain.py`, `tests/pipeline/test_parse.py`, `tests/pipeline/test_snapshot.py`, `tests/pipeline/test_embed.py`, `tests/pipeline/test_analyze.py` | verified | High positive: confirms docs parity against executed test evidence for the rebuilt pipeline. | Keep runbook/operator examples synced to this flow; re-verify after any signature/contract changes in pipeline stages. |
| C049 | `docs/DATA_PIPELINE.md` + `docs/living/CURRENT_STATE.md` | `SubquerySpec.category_filter` supports AND-across-keys and IN-within-key membership filtering over `extra_json` with strict typed equality, eager validation/freezing, and additive canonical emission that preserves legacy `spec_hash` behavior when unset. | `src/study_query_llm/pipeline/types.py`, `src/study_query_llm/pipeline/snapshot.py`, `tests/pipeline/test_snapshot.py` | verified | Medium positive: unlocks snapshot slicing on parser-emitted categorical metadata without parquet schema or parser changes. | Keep typed-matching semantics strict; if coercion or NOT/OR predicates are introduced later, version the filter contract explicitly. |
| C050 | `docs/living/CURRENT_STATE.md` | `twenty_newsgroups` is source-spec + parser backed in the canonical pipeline, acquires the full bydate archive once, parses train+test into labeled rows (parser `v2` no longer applies the legacy `10 < len(text) <= 1000` window in-parser; only null-byte stripping and empty-body drop remain), and exposes parser-emitted top-level keys in parsed `extra_json` (for example `newsgroup` / `split` / `text_len_chars`) so 6-category subsets and length-window selections can be produced at snapshot time; the canonical 6-category convention is centralized via `twenty_newsgroups_6cat_subquery_spec()` (defaults to `label_mode='labeled'` with `category_filter={"newsgroup": list(TWENTY_NEWSGROUPS_6CAT)}`), and the literature-convention text-length window is centralized via `twenty_newsgroups_research_subquery_spec()` (defaults to `label_mode='labeled'` with `filter_expr="text.str.len() > 10 and text.str.len() <= 1000"`, optional `newsgroups=...` composes with the 6-cat subset in a single spec), so independent callers materialize the same `dataset_snapshot` group (identical `spec_hash`). | `src/study_query_llm/datasets/source_specs/twenty_newsgroups.py`, `src/study_query_llm/datasets/source_specs/__init__.py`, `src/study_query_llm/datasets/source_specs/registry.py`, `tests/test_datasets/test_twenty_newsgroups_parser.py`, `tests/pipeline/test_twenty_newsgroups_snapshot.py` | verified | Medium positive: enables one canonical 20 Newsgroups dataframe with flexible downstream subset snapshots, stable parser provenance, and single canonical entry points for both the 6-cat literature subset and the literature-convention length window. | Keep category names/pinning URL stable; bump parser version on text-cleaning or category-metadata contract changes; if the canonical 6-cat default `label_mode` or the canonical research-window bounds change, update the factories and their tests in lockstep so `spec_hash` shifts are intentional. |
| C051 | `docs/living/CURRENT_STATE.md` | `estela` is source-spec + parser backed in the canonical pipeline (parser `v2` no longer applies the legacy `10 < len(text) <= 1000` window in-parser; only null-byte stripping and empty-body drop remain), exposes `extra.text_len_chars` for downstream introspection, and centralizes the literature-convention text-length window via `estela_research_subquery_spec()` (defaults to `label_mode='all'` with `filter_expr="text.str.len() > 10 and text.str.len() <= 1000"`) so independent callers materialize the same `dataset_snapshot` group (identical `spec_hash`). | `src/study_query_llm/datasets/source_specs/estela.py`, `src/study_query_llm/datasets/source_specs/__init__.py`, `tests/pipeline/test_estela_snapshot.py` | verified | Medium positive: aligns estela with twenty_newsgroups on the "parser-canonical, snapshot-slice" pattern and removes a silent in-parser quality gate from the canonical dataframe. | Bump parser version on text-cleaning contract changes; if the canonical research-window bounds change, update the factory and its tests in lockstep so `spec_hash` shifts are intentional. |
| C052 | `docs/review/SNAPSHOT_INVENTORY_2026-04-22.json` + `scripts/snapshot_inventory.py` | Dated snapshot-inventory artifact captures size, label cardinality, character-length percentiles, and canonical `spec_hash` for the four ready-to-run snapshots (`bank77` 13,069 rows / 77 cats; `twenty_newsgroups_6cat` 5,530 / 6; `estela` 324 / 0; `sources_uncertainty_qc_pm` 2,086 / 4) plus two literature-window comparators (`twenty_newsgroups_6cat_research` 1,981 / 6; `estela_research` 286 / 0); regeneration is reproducible via `scripts/snapshot_inventory.py` against current parser identities (banking77 `v1`, twenty_newsgroups `v2`, estela `v2`, sources_uncertainty_qc PM-only `v1`) and pinned acquisition sources (HuggingFace revision, figshare archive, GitHub raw commit, Zenodo record). | `docs/review/SNAPSHOT_INVENTORY_2026-04-22.json`, `scripts/snapshot_inventory.py`, `src/study_query_llm/datasets/source_specs/banking77.py`, `src/study_query_llm/datasets/source_specs/twenty_newsgroups.py`, `src/study_query_llm/datasets/source_specs/estela.py`, `src/study_query_llm/datasets/source_specs/sources_uncertainty_zenodo.py` | verified | Medium positive: provides a date-stamped baseline (row counts, label cardinality, length distributions, `spec_hash` canaries) for the v2 parser refactor and downstream embedding-cost planning; `spec_hash` mismatches on regeneration are a tripwire for unintended canonical-spec or parser changes. | Add a new dated artifact (do not overwrite) on parser bumps or pinning changes; cross-link the new artifact in claims that depend on these row counts. |
| C053 | `docs/runbooks/JETSTREAM_STATE_TIMELINE.md` (linked from `docs/runbooks/README.md`) | Jetstream Postgres lifecycle around the Apr 22 v5 cutover is captured as a neutral chronology: pre-cutover dumps (`jetstream_for_local_20260418_120348Z.dump` 8.3 MB MCQ-rich, `jetstream_for_local_20260422_013304Z.dump` 235 MB), intentional cutover wipe, brief 6-dataset rebuild evidenced by 25 `group_graph_audit_log` rows (banking77, ausem, sources_uncertainty_qc, estela, semeval2013_sra_5way), unexplained second drop, and dormant-state marker `db-backups/jetstream_dormant_post_v5_cutover_20260423_031452Z.dump` (14.7 KB) that closes the chronology gap. The doc states HEAD's `models_v2.py` schema is byte-identical to every dump in the table (verified by empty `git log` on `src/study_query_llm/db/models_v2.py`, `connection_v2.py`, `_base_connection.py`, `migrations/` since 2026-04-22 07:23Z), enumerates three recovery options (Apr 18 restore for MCQ visibility, Apr 22 01:33Z restore for embedding vectors, `db.init_db()` for clean schema with rebuild from blob artifacts), and flags that `scripts/verify_db_backup_inventory.py` exit code 1 against dormant Jetstream is the expected dormancy signal (manifest?blob consistency still validates and prints `OK manifest ?`). | `docs/runbooks/JETSTREAM_STATE_TIMELINE.md`, `docs/runbooks/README.md`, `db-backups/jetstream_dormant_post_v5_cutover_20260423_031452Z.dump` (Azure), `backup_pg_dumps/jetstream_dormant_post_v5_cutover_20260423_031452Z.manifest.json` (operator-local), `src/study_query_llm/db/_base_connection.py`, `src/study_query_llm/db/models_v2.py`, `scripts/verify_db_backup_inventory.py`, `scripts/upload_jetstream_pg_dump_to_blob.py` | verified | Low positive: future operators can reason about Jetstream's empty state and pick a restore path without re-deriving the chronology from `db-backups` filenames and audit-log forensics; explicit dormant-state marker in `db-backups` removes the "absence of dump = unknown state" ambiguity. | Update the timeline section + `Last reviewed` date when Jetstream transitions back to active service (restore performed, `init_db()` rerun, or role officially retired); if `verify_db_backup_inventory.py` is taught to tolerate dormant DBs, remove the corresponding caveat in the runbook. |
| C054 | `docs/API.md` + `docs/runbooks/JETSTREAM_STATE_TIMELINE.md` + `docs/runbooks/README.md` | Destructive DB operations now have a two-layer safety contract in app code: SQLite is allowed by default, non-SQLite destructive calls require explicit `SQLLM_ALLOW_DESTRUCTIVE_DDL=1`, and Jetstream-target matches (`JETSTREAM_DATABASE_URL` normalized host+port+dbname) are hard-stopped even with override; repository-v2 tests are pinned to in-memory SQLite (no `DATABASE_URL` fallback), and regression tests enforce synchronous pre-connect guard behavior on `.invalid` URLs with branch-specific denial expectations. | `src/study_query_llm/db/_base_connection.py`, `tests/test_db/test_repository_v2.py`, `tests/test_db/test_destructive_guard.py`, `docs/API.md`, `docs/runbooks/JETSTREAM_STATE_TIMELINE.md`, `docs/runbooks/README.md` | verified | High positive: blocks the previously observed accidental wipe vector (including tunneled-loopback Jetstream targeting) while preserving explicit operator override for non-Jetstream throwaway targets. | Keep guard/test contract in lockstep: when destructive policy changes, update `_base_connection.py`, `test_destructive_guard.py`, and runbook/API caveats together. |
| C055 | `docs/living/DB_TARGET_CONTRACT.md` + `docs/runbooks/README.md` + `.env.example` + `docs/living/API_CURRENT.md` + `scripts/README.md` + `docs/living/CURRENT_STATE.md` | A prep-only Stage C/D/E contract is now explicit and versioned: DB Target Contract v1 defines env-var ownership (`DATABASE_URL`, `JETSTREAM_DATABASE_URL`, `LOCAL_DATABASE_URL`, `SOURCE_DATABASE_URL`, plus staged `PANEL_DATABASE_URL`, `TEST_DATABASE_URL`, `SCRATCH_DATABASE_URL`), caller-specific precedence, Stage C/D/E file-touchpoint map, no-network-safe test matrix, PR slicing with rollback checkpoints, and stop/escalation triggers; `PIPELINE_DATABASE_URL` is explicitly deferred to contract v2. Documentation now links this contract from runbook/API/scripts/current-state surfaces and includes staged env placeholders in `.env.example` without claiming runtime wiring has already landed. | `docs/living/DB_TARGET_CONTRACT.md`, `docs/runbooks/README.md`, `.env.example`, `docs/living/API_CURRENT.md`, `scripts/README.md`, `docs/living/CURRENT_STATE.md` | verified | Medium positive: preserves topology flexibility while preventing contract drift ahead of implementation, giving future C/D/E work a single non-ambiguous acceptance baseline and rollback model. | Keep v1/v1.1/v2 boundaries explicit; when C/D/E code lands, update this claim and add implementation-parity claims tied to resolver call sites/tests. |
| C056 | `docs/living/CURRENT_STATE.md` + `docs/design/clustering_pipeline_provenance.md` | Clustering provenance v1 is implemented in `analyze`: YAML-backed resolver (`config/rules/clustering/rules-v1.0.0.yaml`), pre-run + post-selection validators, effective-pipeline hashing (`pipeline_effective_hash`), identity contract (`recipe_hash == pipeline_effective_hash` when materialized), and v1 envelope fields written in `structured_results.clustering_summary` plus execution `config_json`; canonical runners for `hdbscan`, `kmeans+silhouette+kneedle`, and `gmm+bic+argmin` are active with selection evidence artifacts for sweep methods and trigger-1 backward-compatible `operation_type` uplift in HDBSCAN summary payloads. | `src/study_query_llm/pipeline/clustering/*.py`, `config/rules/clustering/rules-v1.0.0.yaml`, `src/study_query_llm/pipeline/analyze.py`, `src/study_query_llm/pipeline/hdbscan_runner.py`, `src/study_query_llm/algorithms/recipes.py`, `scripts/register_clustering_methods.py`, `scripts/run_bank77_pipeline.py`, `tests/pipeline/test_clustering_provenance.py`, `tests/pipeline/test_analyze.py`, `tests/pipeline/test_bank77_pipeline_e2e.py`, `tests/test_services/test_recipe.py` | verified | High positive: materially improves clustering reproducibility/auditability while preserving legacy payload readers during forward-write cutover. | Keep rule-set/version changes explicit (hash + version bump), and add new parity claims when additional clustering composites or rule sets are introduced. |
| C057 | `docs/living/CURRENT_STATE.md` + `docs/runbooks/README.md` + `docs/living/API_CURRENT.md` + `scripts/README.md` + `.env.example` | DB lane guardrails are now end-to-end enforceable in runtime and ops surfaces: v2 constructors enforce explicit non-sqlite write intent + lane compatibility; canonical `call_artifacts.uri` is protected by validated DB constraint (`call_artifacts_uri_must_be_blob`) with remediation tooling (`scripts/remediate_call_artifacts_to_blob.py`); canonical-intent artifact writes fail closed to Azure Blob (`ArtifactService(write_intent=WriteIntent.CANONICAL)` rejects local backend); raw-call URI sentinel index/monitor surfaces exist (`add_raw_calls_uri_sentinel_index.py`, `check_raw_calls_uri_sentinel.py`); and CI runs DB lane static policy lint (`scripts/check_db_lane_policy.py` in `persistence-contract.yml`). | `src/study_query_llm/db/_base_connection.py`, `src/study_query_llm/db/write_intent.py`, `src/study_query_llm/db/migrations/add_call_artifacts_blob_uri_check.py`, `scripts/remediate_call_artifacts_to_blob.py`, `scripts/check_call_artifacts_uri_constraint.py`, `src/study_query_llm/services/artifact_service.py`, `src/study_query_llm/pipeline/runner.py`, `src/study_query_llm/db/migrations/add_raw_calls_uri_sentinel_index.py`, `scripts/check_raw_calls_uri_sentinel.py`, `scripts/check_db_lane_policy.py`, `.github/workflows/persistence-contract.yml`, `.env.example`, `tests/test_db/test_call_artifacts_blob_uri_constraint_migration.py`, `tests/test_db/test_raw_calls_uri_sentinel_index_migration.py`, `tests/test_services/test_artifact_service.py`, `tests/test_scripts/test_check_db_lane_policy.py` | verified | High positive: materially reduces canonical-target ambiguity and local-path URI regressions by coupling DB intent, artifact backend policy, DB constraints, remediation tooling, and CI policy checks. | Keep docs + CI + migration monitors aligned whenever intent vocabulary, URI contracts, or allowed `create_engine` policy surfaces change. |
| C058 | `docs/living/CURRENT_STATE.md` + `docs/USER_GUIDE.md` | Panel safe-mode runtime currently disables the **Inference** tab while keeping analytics/embeddings/groups/storage/sweep explorer tabs active. | `panel_app/app.py`, `docs/living/CURRENT_STATE.md`, `docs/USER_GUIDE.md` | verified | Medium positive: reduces accidental write-risk from interactive inference while preserving read/exploration surfaces. | Revisit when provider/runtime contracts for panel inference are aligned and tab can be re-enabled intentionally. |
| C059 | `docs/runbooks/README.md` + `docs/living/CURRENT_STATE.md` + `scripts/README.md` + `.env.example` | Full-state Jetstream backup has a single automation entrypoint: `scripts/backup_jetstream_full_state.py` orchestrates dump/create (`dump_postgres_for_jetstream_migration.py --from-jetstream`), dump upload (`upload_jetstream_pg_dump_to_blob.py`), strict inventory verification (`verify_db_backup_inventory.py --strict-azure`), artifact-container mirroring to a dated backup prefix (default destination `<source>-backups`, optional cross-account destination via `AZURE_BACKUP_STORAGE_CONNECTION_STRING`), and local receipt emission under `backup_pg_dumps/`; dry-run mode performs no writes. | `scripts/backup_jetstream_full_state.py`, `tests/test_scripts/test_backup_jetstream_full_state.py`, `docs/runbooks/README.md`, `docs/living/CURRENT_STATE.md`, `scripts/README.md`, `.env.example` | verified | High positive: removes manual multi-step/operator drift risk and provides a repeatable, receipt-backed backup workflow that includes both DB state and blob artifacts. | Keep defaults aligned with artifact container routing (`ARTIFACT_RUNTIME_ENV` + `AZURE_STORAGE_CONTAINER`) and update runbook options when backup topology/env vars change. |

## Severity-Ranked Mismatch Summary

### Open High

- None

### Open Medium

- None

### Open Low

- None

## Aligned and Sampled Areas

These areas are sampled and considered sufficiently aligned for this review cycle:

- Sweep worker and package CLI operational architecture (`docs/ARCHITECTURE.md` + `src/study_query_llm/cli` + `runtime_*` modules).
- Sweep migration runbook CLI-first direction and wrapper model (`docs/SWEEP_MIGRATION_RUNBOOK.md` + `scripts/run_*.py` wrappers).
- Local DB clone runbook structure and script references (`docs/LOCAL_DB_CLONE_FROM_JETSTREAM.md` + dump/restore scripts).

## Notes

- This ledger is intentionally claim-centric and does not replace architecture docs.
- Hypotheses that need runtime/load validation are tracked in `docs/DESIGN_FLAWS.md`.

