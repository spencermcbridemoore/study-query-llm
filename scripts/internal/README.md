# Scripts Internal Lane

Place helper modules here when they are imported by scripts but not intended as
standalone operator entrypoints.

Current helper modules may still live at script root for compatibility and can be
migrated here in future cleanup passes.
